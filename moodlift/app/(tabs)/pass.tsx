import { Text } from 'react-native';
import { Screen } from '../../components/ui/Screen';

export default function PassScreen() {
  return (
    <Screen>
      <Text className="font-display text-3xl text-ink">Gym pass</Text>
    </Screen>
  );
}
