import { Text } from 'react-native';
import { Screen } from '../../components/ui/Screen';

export default function WorkoutsScreen() {
  return (
    <Screen>
      <Text className="font-display text-3xl text-ink">Workouts</Text>
    </Screen>
  );
}
