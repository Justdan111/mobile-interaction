import { Text } from 'react-native';
import { Screen } from '../../components/ui/Screen';

export default function HomeScreen() {
  return (
    <Screen>
      <Text className="font-display text-3xl text-ink">Home</Text>
    </Screen>
  );
}
