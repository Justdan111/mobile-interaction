// The design is set in DM Sans. React Native doesn't synthesise weights for
// custom families, so each weight is its own family name.
export const font = {
  regular: 'DMSans_400Regular',
  medium: 'DMSans_500Medium',
  semibold: 'DMSans_600SemiBold',
  bold: 'DMSans_700Bold',
} as const;
